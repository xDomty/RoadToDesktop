#include <iostream>
using namespace std;
int main()
{
//Constants are like a lock that make the virable can not change again
//example
const int x = 1;
//now i can not change the value if x , and if i change it the compiler gets error
  //returning 0
     return 0;
}