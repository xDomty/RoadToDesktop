#include <iostream>
#include <cmath>
using namespace std;
int main()
{
   short int a=4;
   short int sqrtA = sqrt(a); //sqrt function is the root
   short int RoundA = round(a); // The round function rounds a decimal number to an integer.
   short int Power2ForA = pow(a,2); //the pow function powers the number
   short int CeilA = ceil(a); //celi function always rounds a decimal number to an int bigger number 
   short int FloarA = floor(a); //floor function always rounds a decimal number to an int smaller number
   short int AbsA = abs(a); //makes any number always be positive
   system("pause");
}