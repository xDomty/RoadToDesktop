#pragma once
#include <iostream>
using namespace std;

namespace Swap
{
	void swap(float& a, float& b);

	void swap(float* a, float* b);

	void swap(int* a, int* b);

	void swap(int& a, int& b);
};
